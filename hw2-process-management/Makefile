# =========================================================
# PATCH for kernel/Makefile
# =========================================================
# Find the obj-y line that ends with:
#     async.o range.o smpboot.o ucount.o
# and append hw2.o to it, like this:
#
#     async.o range.o smpboot.o ucount.o hw2.o
#
# That's the only change needed in this file.
# =========================================================
